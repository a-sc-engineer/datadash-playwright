name: QA Test 25ds3000057@ds.study.iitm.ac.in

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - run: pip install playwright

    - run: playwright install

    - run: python sum.py
